---
test_name: Get some events from the JSON events API

stages:
  - name: Get event titles from the connected db
    request:
      url: http://staging-raceready.eu-gb.mybluemix.net/api/events/
      method: GET
    response:
      status_code: 200
